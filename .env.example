# Optional. Raises GitHub Search API rate limits. The app works without it.
GITHUB_TOKEN=

# Google Apps Script web app URL (/exec) that appends rows to the Pulse email signups Sheet.
# Required for the top-of-page signup form.
PULSE_SIGNUP_WEBAPP_URL=
